# Frontend (Next.js)

Simple Next.js frontend for the e-portfolio MVP.

Run (PowerShell):

```powershell
cd "c:\Users\user\AppData\Local\Android\Sdk\extras\portfolio-app\frontend"
npm install
npm run dev
```

The frontend proxies download requests to the backend to preserve auth headers.
